import React from "react";
import {StyledButton} from "./styles";

function SuscribeButton() {
    return (
        <StyledButton
            type="submit"
            value="Subscribe"
            aria-label="submit button for newsletter"></StyledButton>
    );
}

export default SuscribeButton;
